const series = require("../series") // modulo propio
const fs = require ("fs")   //modulos nativos
const moment = require("moment") //modulo de terceros
const express = require ("express")


console.log(express);